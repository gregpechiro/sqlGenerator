package main

/*func GetUserById(Id int) User {
	user := User{}

	err := db.QueryRow("SELECT * FROM user WHERE id=?", Id).Scan(&user.Id, &user.Name, &user.Age, &user.Active)
	if err != nil {
		fmt.Printf("GetUserById >> %v\n", err)
	}
	return user
}*/
